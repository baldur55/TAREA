#! TERCERA SEMANA, EJERCICIO 7 - CICLOFOR
class For:
    def __init__(self):
        pass
    
      #? ciclo repetitivo de incremento o decrementos se ejecuta mientas tenga valores 
    def usoFor(self):
        datos=["Daniel",50,true]
        numeros=(2,5,6,4,1)
        docente = {'nombre': 'daniel','edad':50,'fac':'faci'}
        listaNotas = [(30,40).(20,40),(50,40)]
        listaAlumnos = [{"nombre":"Erick","final":70},{"nombre":"Yady","final":50},{"nombre":"Danny","final":}]
        #! range ([inicio=0],limite,[inc/dec=1]).Genera un rango de valores desde un valor inicial a un valos final
        #* se ejecuta desde inicio hasta el limite 
           listaNotas = [(30,40),[20,40,20],(50,40,20,10)]
           acum=0
           long=0
        for notas in listaNotas:
          parcial=0
          print(notas,end="")
             for nota in notas:
                 print(notas)
                 long=long+1
                 acun = acum + nota
                 parcial == nota
                promParcial = parcial/len(notas)
                print('notas Parciales ={} Promedio Parciales={}'.format(parcial,promParcial))
             prom = acum/long
             print("total notas={} - Notas={}: Promedio={}",format(acu,long,prom))
             
                      
                 
bucle1= For()
bucle1.usoFor()